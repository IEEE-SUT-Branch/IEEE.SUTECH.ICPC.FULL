# Domain used for public access and TLS certificate
DOMAIN=your-domain.com
LETSENCRYPT_EMAIL=you@example.com

# Build-time frontend API URL. Keep /api when proxied through nginx/frontend.
VITE_API_URL=/api

# Frontend container local host binding (Caddy will expose 80/443 publicly)
APP_PORT=8080
